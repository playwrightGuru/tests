name: "e2e"
on: [push]
jobs:
 test-e2e:
  name: Run E2E tests
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v2
      with:
       node-version: '14'
    - name: Install dependencies
      run: npm install
    - name: Install Playwright
     # run: npx playwright install --with-deps
      run: pnpm dlx playwright-core install chromium
    - name: Run your tests
      run: npx playwright test tests/home.spec.ts
    #- name: Upload test results
    # if: always()
    # uses: actions/upload-artifactory@v2
    # with:
    #   name: Test results
    #   path: test-results